# Core FastAPI dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# Image processing
Pillow>=10.2.0  # Updated to version with pre-built wheels for Windows
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0  # Compatible with torch and other packages

# CLIP for semantic embeddings
# Note: Install CLIP using git (works with newer torch versions)
# Run: pip install git+https://github.com/openai/CLIP.git
# Or install ftfy and regex manually if needed
ftfy>=6.0.0
regex>=2022.0.0
tqdm>=4.0.0

# Vector database
faiss-cpu>=1.7.4  # Use faiss-gpu if you have CUDA

# Database (optional for MVP, but included for future)
psycopg2-binary>=2.9.9  # PostgreSQL
python-dotenv>=1.0.0

# Utilities
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Machine Learning (for feature extraction)
scikit-learn>=1.3.0
scikit-image>=0.21.0

# Graph database (for product relationships)
networkx>=3.0



